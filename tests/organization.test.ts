import {test} from 'node:test';import assert from 'node:assert/strict';
import {matchOrganization,normalizeDomain,organizationChoiceSchema} from '../packages/shared/src/organization';
const customers=[{id:'a',name:'NYC Luxury',domain:'nycluxury.example',aliases:['NYC Luxury Inc']},{id:'b',name:'Other Customer',domain:'other.example',aliases:[]}];
test('domain and exact aliases resolve customer identity',()=>{assert.equal(normalizeDomain('https://www.NYCLuxury.example/path'),'nycluxury.example');assert.equal(matchOrganization({name:'NYC Luxury Inc',domain:null,confidence:'high'},customers).match,'a');assert.equal(matchOrganization({name:null,domain:'nycluxury.example',confidence:'high'},customers).match,'a');});
test('conflicting domain/name and duplicate names cannot silently merge customers',()=>{assert.equal(matchOrganization({name:'NYC Luxury',domain:'other.example',confidence:'high'},customers).ambiguous,true);assert.equal(matchOrganization({name:'NYC Luxury',domain:'different.example',confidence:'high'},customers).match,null);assert.equal(matchOrganization({name:'NYC Luxury',domain:null,confidence:'high'},[...customers,{...customers[0],id:'c'}]).match,null);});
test('similar names are not identity matches; manual choice needs a valid id or name',()=>{assert.equal(matchOrganization({name:'NYC Luxury Watches',domain:null,confidence:'low'},customers).match,null);assert.equal(organizationChoiceSchema.safeParse({organizationId:'unknown'}).success,false);assert.equal(organizationChoiceSchema.safeParse({name:'',domain:null}).success,false);});
