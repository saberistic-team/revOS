FROM python:3.12-slim
WORKDIR /app
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt && useradd -u 1000 app
COPY . .
USER 1000
ENV PORT=8080
EXPOSE 8080
CMD ["python", "app.py"]
